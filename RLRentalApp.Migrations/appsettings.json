{
  // Uncomment and replace these values when you want this project to connect directly to PostgreSQL without Aspire or environment variables.
  // Keep real passwords out of git; prefer user-secrets locally or ConnectionStrings__rentaldb in production.
  // "ConnectionStrings": {
  //   "rentaldb": "Host=localhost;Port=5432;Database=rentaldb;Username=postgres;Password=your-password;SSL Mode=Prefer;Trust Server Certificate=true"
  // },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  }
}
